import 'package:flutter/material.dart';

class Aporaisal extends StatelessWidget {
  const Aporaisal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
